---
---

<section class="promise-section">
  <div class="promise-container">
    <div class="promise-heading">Our Promise to You</div>
    <p class="promise-subtext">
      You’ll get scheduled quickly. You’ll know exactly what you’re paying. Your scan will be done right — or we’ll make it right. Guaranteed.
    </p>

    <div class="promise-cards">
      <div class="promise-card">
        <div class="icon">🛡️</div>
        <div>
          <p class="promise-title">100% Quality Guarantee</p>
          <p class="promise-detail">We vet every scan. If it’s not right, we’ll fix it — no cost to you.</p>
        </div>
      </div>

      <div class="promise-card">
        <div class="icon">⏱️</div>
        <div>
          <p class="promise-title">Fast Turnaround</p>
          <p class="promise-detail">Most appointments happen same or next day, with results in 24–48 hours.</p>
        </div>
      </div>

      <div class="promise-card">
        <div class="icon">🤝</div>
        <div>
          <p class="promise-title">We’re With You</p>
          <p class="promise-detail">Real people, real support — at every step, with your best interest first.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .promise-section {
    background: #0e1b2a;
    color: white;
    padding: 4rem 1rem;
    text-align: center;
  }
  .promise-container {
    max-width: 1200px;
    margin: 0 auto;
  }
  .promise-heading {
    font-size: 2rem;
    font-weight: 600;
    color: #e6c378;
    margin-bottom: 1rem;
  }
  .promise-subtext {
    color: #cbd5e1;
    font-size: 1.125rem;
    max-width: 640px;
    margin: 0 auto 3rem;
  }
  .promise-cards {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
  .promise-card {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    text-align: left;
    justify-content: center;
  }
  .icon {
    font-size: 1.75rem;
    color: #e6c378;
    flex-shrink: 0;
  }
  .promise-title {
    font-weight: 600;
    color: white;
    margin-bottom: 0.25rem;
  }
  .promise-detail {
    color: #94a3b8;
    font-size: 0.875rem;
  }
  @media (min-width: 768px) {
    .promise-cards {
      flex-direction: row;
    }
  }
</style>
