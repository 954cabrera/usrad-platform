<!-- HeroSmartSplit.astro - Hero Only -->
<section class="py-24 bg-[#f0f4f9] text-gray-800 relative overflow-hidden">
  <!-- Subtle pattern overlay -->
  <div class="absolute inset-0 bg-repeat opacity-[0.03]" style="background-image: url('/images/subtle-pattern.svg');"></div>
  
  <!-- Decorative elements -->
  <div class="absolute top-20 left-20 w-64 h-64 rounded-full bg-[#e6c378] opacity-[0.03] blur-3xl"></div>
  <div class="absolute bottom-20 right-20 w-72 h-72 rounded-full bg-[#003087] opacity-[0.03] blur-3xl"></div>
  
  <div class="px-6 sm:px-8 lg:px-12">
    <div class="max-w-7xl mx-auto relative z-10">
      <div class="grid md:grid-cols-2 gap-16 items-center">
        <!-- Left column - preserved original headline and text content -->
        <div class="space-y-6" data-aos="fade-right">
          <h1 class="text-4xl md:text-5xl lg:text-6xl font-semibold text-[#003087] leading-tight">
            Simpler imaging partnerships.
          </h1>
          <h2 class="text-3xl md:text-4xl lg:text-5xl font-medium text-[#003087] leading-tight">
            Stronger patient pipelines.
          </h2>
          <div class="h-1 w-48 bg-[#e6c378] rounded-full"></div>
          <p class="text-xl text-gray-700 mt-6">
            USRad connects your center to thousands of self-pay patients—no
            referrals required, no tech overhead. Schedule faster. Get paid quicker.
            Expand smarter.
          </p>
          <div class="mt-8">
            <a href="/partner/apply" class="inline-block bg-[#003087] text-white px-8 py-4 rounded-full hover:bg-[#002266] transition-all hover:shadow-lg border-l-4 border-[#e6c378] pulse-button">
              Apply Now to Join the Network
            </a>
          </div>
        </div>
        
        <!-- Right column - preserved original content in luxury format -->
        <div class="relative" data-aos="fade-left">
          <div class="absolute -top-4 -right-4 w-24 h-24 rounded-full bg-[#e6c378] opacity-20"></div>
          <div class="relative bg-white p-6 rounded-3xl shadow-xl overflow-hidden max-w-md mx-auto space-y-6 z-10">
            <img
              src="/images/hero-radiologist-reading-brain.png"
              alt="Radiologist reading brain MRI"
              class="w-full h-56 object-cover rounded-xl shadow-md"
            />

            <!-- UI card: SmartMatch -->
            <div
              class="bg-white p-4 rounded-xl shadow-md flex items-center justify-between border-l-2 border-[#e6c378]"
            >
              <div>
                <p class="text-sm text-gray-500">New SmartMatch™</p>
                <p class="text-md font-semibold text-gray-900">Patient: Lisa D.</p>
                <p class="text-xs text-[#003087]">Location: Atlanta, GA</p>
              </div>
              <button class="text-sm bg-[#f0f4f9] text-[#003087] px-3 py-1 rounded-lg hover:bg-[#e6c378] hover:text-white transition-colors"
                >View</button
              >
            </div>

            <!-- Metrics -->
            <div class="flex justify-between text-center text-sm text-gray-600">
              <div>
                <p class="font-bold text-lg text-[#003087]">82%</p>
                <p>Faster Pay</p>
              </div>
              <div>
                <p class="font-bold text-lg text-[#003087]">12 min</p>
                <p>Avg Scheduling</p>
              </div>
              <div>
                <p class="font-bold text-lg text-[#003087]">4.9★</p>
                <p>Center Rating</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Visual element for continuity with next section -->
  <div class="absolute bottom-0 left-0 right-0 h-16 bg-gradient-to-b from-transparent to-[#fcf9f0] opacity-30"></div>
</section>

<style>
  /* Pulsating effect */
  .pulse-button {
    animation: pulse 2s infinite ease-in-out;
  }

  /* Keyframes for the pulsating animation */
  @keyframes pulse {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
    100% {
      transform: scale(1);
    }
  }
</style>
