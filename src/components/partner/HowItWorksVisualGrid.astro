---
const steps = [
  {
    step: 1,
    title: "Simple Application",
    description: "Complete our streamlined application. We'll confirm your qualifications and align on standards.",
    icon: "📄",
    duration: "Typically less than 7 days"
  },
  {
    step: 2,
    title: "Seamless Integration",
    description: "We handle the technical setup so appointments flow smoothly into your system.",
    icon: "🔧",
    duration: "Setup in 1–2 business days"
  },
  {
    step: 3,
    title: "Patient Referrals",
    description: "Receive patient appointments directly, complete with all necessary information.",
    icon: "📅",
    duration: "Referrals begin immediately"
  },
  {
    step: 4,
    title: "Seamless Payment",
    description: "Get paid directly—no insurance hurdles, no delays.",
    icon: "💳",
    duration: "Payments within 48 hours"
  },
];
---

<section class="bg-[#fcf9f0] py-24 text-[#003087] px-6 relative overflow-hidden">
  <div class="max-w-7xl mx-auto text-center mb-16">
    <h2 class="text-4xl font-bold flex items-center justify-center">
      <span class="w-2 h-10 bg-[#e6c378] mr-3 rounded-full"></span>
      The Partnership Journey
    </h2>
    <p class="text-lg text-gray-600 mt-4 italic">
      From application to payment, here’s what working with USRad looks like.
    </p>
  </div>

  <div class="space-y-12 max-w-4xl mx-auto">
    {steps.map(({ step, title, description, icon, duration }) => (
      <div class="flex items-start gap-6 text-left">
        <!-- Step Number -->
        <div class="flex-shrink-0 flex items-center justify-center w-12 h-12 bg-[#003087] text-white rounded-full text-xl font-bold shadow-md">
          {step}
        </div>

        <!-- Content Card -->
        <div class="bg-white rounded-xl shadow-md p-6 flex-1 border border-gray-100 hover:shadow-lg transition">
          <p class="text-sm text-gray-500 font-medium mb-2">Step {step} of 4</p>
          <h3 class="text-2xl font-semibold flex items-center">
            <span class="inline-block mr-2 text-2xl">{icon}</span> {title}
          </h3>
          <p class="mt-2 text-gray-700">{description}</p>
          <div class="mt-4 flex items-center text-sm text-gray-500">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-[#e6c378]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <span>{duration}</span>
          </div>
        </div>
      </div>
    ))}
  </div>

  <div class="text-center mt-20">
    <p class="text-lg font-medium">Ready to get started?</p>
    <p class="text-sm text-gray-600 mb-6">Fast onboarding. Transparent terms. No surprises.</p>
    <a href="/partner/apply" class="bg-[#003087] text-white py-3 px-8 rounded-full text-base shadow-lg hover:bg-[#002266] transition border-l-4 border-[#e6c378]">
      Apply to Join Now
    </a>
  </div>
</section>
