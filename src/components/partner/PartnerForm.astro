<section id="form" class="bg-gray-50 py-28 px-6">
  <div class="max-w-3xl mx-auto text-center space-y-10" data-aos="fade-up">
    <h2 class="text-4xl font-bold text-gray-900">
      Ready to Join the Network?
    </h2>
    <p class="text-lg text-gray-600">
      Let us know a bit about your center. We’ll be in touch with next steps and how you can start receiving SmartMatch™ referrals.
    </p>

    <form name="partner-interest" method="POST" netlify class="space-y-6 text-left">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <input type="text" name="name" placeholder="Your Name" class="input" required />
        <input type="email" name="email" placeholder="Email Address" class="input" required />
        <input type="text" name="center" placeholder="Imaging Center Name" class="input" required />
        <input type="tel" name="phone" placeholder="Phone Number" class="input" />
      </div>

      <textarea
        name="message"
        rows="4"
        placeholder="Tell us more about your center..."
        class="input w-full"
      ></textarea>

      <div class="text-center">
        <button
          type="submit"
          class="bg-blue-700 hover:bg-blue-800 text-white font-semibold py-3 px-8 rounded-full transition"
        >
          Submit Interest
        </button>
      </div>
    </form>
  </div>
</section>
