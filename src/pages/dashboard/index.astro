---
// src/pages/dashboard/index.astro - ONLY ADD PSA STATUS
import DashboardLayout from "../../components/dashboard/DashboardLayout.astro";
import SkeletonProviderDashboardSystem from "../../components/dashboard/SkeletonProviderDashboardSystem";

const fullyOnboarded = false;

// Add PSA status to your existing mockData
const mockData = {
  todayAppointments: 8,
  totalAppointments: 124,
  completedScans: 98,
  totalRevenue: 47500,
  pendingReports: 12,
  avgScanTime: "28 min",
  patientSatisfaction: 4.8,
  equipmentStatus: "Operational",
  // ADD THESE TWO LINES:
  hasCompletedPSA: false, // Change to true to test unlock
  onboardingProgress: 75,
};
---

<DashboardLayout title="Dashboard Overview" user={null} imagingCenter={null}>
  <SkeletonProviderDashboardSystem
    client:load
    user={null}
    imagingCenter={null}
    mockData={mockData}
    fullyOnboarded={fullyOnboarded}
  />
</DashboardLayout>
