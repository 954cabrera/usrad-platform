---
// src/pages/dashboard/onboarding/index.astro
import DashboardLayout from "../../../components/dashboard/DashboardLayout.astro";
import { validateAuthSession } from "../../../lib/auth";

// Validate authentication
const { session, user } = await validateAuthSession(Astro.request);

if (!session || !user) {
  return Astro.redirect("/login/imaginglogin");
}

// Mock onboarding status - replace with real data from your database
const onboardingStatus = {
  facilities: { completed: false, completedDate: null, status: "pending" },
  psa: { completed: false, completedDate: null, status: "locked" },
  caqh: { completed: false, completedDate: null, status: "locked" },
  banking: { completed: false, completedDate: null, status: "locked" },
  welcome: { completed: false, completedDate: null, status: "locked" },
};

// Calculate overall progress
const steps = Object.values(onboardingStatus);
const completedSteps = steps.filter((step) => step.completed).length;
const totalSteps = steps.length;
const progressPercentage = Math.round((completedSteps / totalSteps) * 100);
---

<DashboardLayout title="Provider Onboarding" user={user}>
  <!-- Custom styles matching USRad theme -->
  <style>
    .usrad-gradient-bg {
      background: linear-gradient(135deg, #fcf9f0 0%, #f8f5e8 100%);
      background-image: radial-gradient(
          circle at 25% 25%,
          rgba(0, 48, 135, 0.03) 0%,
          transparent 50%
        ),
        radial-gradient(
          circle at 75% 75%,
          rgba(204, 153, 51, 0.03) 0%,
          transparent 50%
        );
    }

    .usrad-card {
      background: white;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      border: 1px solid rgba(0, 48, 135, 0.08);
      transition: all 0.3s ease;
    }

    .usrad-card:hover {
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12);
      transform: translateY(-2px);
    }

    .usrad-navy {
      color: #003087;
    }
    .usrad-gold {
      color: #cc9933;
    }
    .bg-usrad-navy {
      background-color: #003087;
    }
    .bg-usrad-gold {
      background-color: #cc9933;
    }

    .usrad-gradient-navy {
      background: linear-gradient(135deg, #003087 0%, #001a4d 100%);
    }

    .usrad-gradient-gold {
      background: linear-gradient(135deg, #cc9933 0%, #e6b84d 100%);
    }

    .step-card {
      transition: all 0.3s ease;
      cursor: pointer;
    }

    .step-card.completed {
      background: linear-gradient(135deg, #10b981, #059669);
      color: white;
    }

    .step-card.active {
      background: linear-gradient(135deg, #003087, #001a4d);
      color: white;
    }

    .step-card.locked {
      background: #f3f4f6;
      color: #9ca3af;
      cursor: not-allowed;
    }

    .step-card.locked:hover {
      transform: none;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
    }

    .progress-bar {
      background: linear-gradient(90deg, #10b981, #059669);
      transition: width 0.6s ease-in-out;
    }

    .animate-fade-in-up {
      animation: fadeInUp 0.6s ease-out forwards;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .step-icon {
      width: 60px;
      height: 60px;
      border-radius: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1rem;
    }

    .step-number {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      margin-right: 1rem;
    }
  </style>

  <!-- Welcome Header -->
  <div
    class="usrad-card p-8 mb-8 usrad-gradient-navy text-white relative overflow-hidden animate-fade-in-up"
  >
    <div
      class="absolute top-0 right-0 w-64 h-64 bg-white/5 rounded-full -translate-y-32 translate-x-32"
    >
    </div>
    <div
      class="absolute bottom-0 left-0 w-48 h-48 bg-white/5 rounded-full translate-y-24 -translate-x-24"
    >
    </div>
    <div class="relative z-10">
      <h1
        class="text-4xl font-bold mb-3"
        style="font-family: 'Manrope', sans-serif;"
      >
        Welcome to USRad! 🎉
      </h1>
      <p
        class="text-blue-100 text-xl mb-6"
        style="font-family: 'Manrope', sans-serif;"
      >
        Let's get you onboarded to start providing diagnostic imaging services
        in our network.
      </p>
      <div class="flex items-center space-x-8">
        <div class="flex items-center space-x-3">
          <div class="w-4 h-4 bg-blue-400 rounded-full animate-pulse"></div>
          <span class="text-blue-100 font-medium"
            >Your onboarding is in progress</span
          >
        </div>
        <div class="text-blue-100">
          {completedSteps} of {totalSteps} steps completed
        </div>
      </div>
    </div>
  </div>

  <!-- Progress Overview -->
  <div
    class="usrad-card p-8 mb-8 animate-fade-in-up"
    style="animation-delay: 0.1s;"
  >
    <div class="flex items-center justify-between mb-6">
      <div>
        <h2
          class="text-2xl font-bold usrad-navy"
          style="font-family: 'Manrope', sans-serif;"
        >
          Onboarding Progress
        </h2>
        <p class="text-gray-600 mt-1">
          Complete all steps to activate your provider account
        </p>
      </div>
      <div class="text-right">
        <div
          class="text-4xl font-bold usrad-gold"
          style="font-family: 'Manrope', sans-serif;"
        >
          {progressPercentage}%
        </div>
        <div class="text-gray-600 font-medium">Complete</div>
      </div>
    </div>

    <div class="w-full bg-gray-200 rounded-full h-4 mb-4">
      <div
        class="progress-bar h-4 rounded-full"
        style={`width: ${progressPercentage}%`}
      >
      </div>
    </div>

    <div class="text-sm text-gray-600">
      {
        completedSteps === totalSteps
          ? "🎉 Congratulations! Your onboarding is complete."
          : `${totalSteps - completedSteps} step${totalSteps - completedSteps !== 1 ? "s" : ""} remaining`
      }
    </div>
  </div>

  <!-- Onboarding Steps -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
    <!-- Step 1: Facility Locations Setup -->
    <div
      class={`usrad-card p-8 step-card ${onboardingStatus.facilities.completed ? "completed" : "active"} animate-fade-in-up`}
      style="animation-delay: 0.2s;"
      onclick={onboardingStatus.facilities.completed
        ? ""
        : "window.location.href='/dashboard/onboarding/facilities'"}
    >
      <div class="flex items-start">
        <div class="step-number bg-white/20">
          {
            onboardingStatus.facilities.completed ? (
              <svg
                width="20"
                height="20"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"
                />
              </svg>
            ) : (
              <span>1</span>
            )
          }
        </div>
        <div class="flex-1">
          <h3
            class="text-xl font-bold mb-2"
            style="font-family: 'Manrope', sans-serif;"
          >
            Facility Locations Setup
          </h3>
          <p class="mb-4 opacity-90">
            Add all imaging center locations that will be included in your
            Provider Service Agreement.
          </p>
          <div class="space-y-2 text-sm opacity-80">
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-4m-5 0H3m2 0h3M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"
                ></path>
              </svg>
              Support for 1 to 1000+ locations
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"
                ></path>
              </svg>
              Bulk CSV upload available
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Est. 5-15 minutes
            </div>
          </div>
          {
            onboardingStatus.facilities.completed && (
              <div class="mt-4 text-sm">
                ✅ Completed on {onboardingStatus.facilities.completedDate}
              </div>
            )
          }
        </div>
      </div>
    </div>

    <!-- Step 2: Provider Service Agreement -->
    <div
      class={`usrad-card p-8 step-card ${
        onboardingStatus.psa.completed
          ? "completed"
          : onboardingStatus.facilities.completed
            ? "active"
            : "locked"
      } animate-fade-in-up`}
      style="animation-delay: 0.3s;"
      onclick={onboardingStatus.facilities.completed &&
      !onboardingStatus.psa.completed
        ? "window.location.href='/dashboard/onboarding/psa'"
        : ""}
    >
      <div class="flex items-start">
        <div
          class={`step-number ${
            onboardingStatus.psa.completed
              ? "bg-white/20"
              : onboardingStatus.facilities.completed
                ? "bg-white/20"
                : "bg-gray-300"
          }`}
        >
          {
            onboardingStatus.psa.completed ? (
              <svg
                width="20"
                height="20"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"
                />
              </svg>
            ) : (
              <span>2</span>
            )
          }
        </div>
        <div class="flex-1">
          <h3
            class="text-xl font-bold mb-2"
            style="font-family: 'Manrope', sans-serif;"
          >
            Provider Service Agreement
          </h3>
          <p class="mb-4 opacity-90">
            Review and digitally sign your PSA with all facility locations
            included.
          </p>
          <div class="space-y-2 text-sm opacity-80">
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"
                ></path>
              </svg>
              100% Medicare rates
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Includes your facility locations
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Est. 5-10 minutes
            </div>
          </div>
          {
            !onboardingStatus.facilities.completed && (
              <div class="mt-4 text-sm opacity-60">
                🔒 Complete facility setup first
              </div>
            )
          }
          {
            onboardingStatus.psa.completed && (
              <div class="mt-4 text-sm">
                ✅ Completed on {onboardingStatus.psa.completedDate}
              </div>
            )
          }
        </div>
      </div>
    </div>

    <!-- Step 3: CAQH Credentialing -->
    <div
      class={`usrad-card p-8 step-card ${
        onboardingStatus.caqh.completed
          ? "completed"
          : onboardingStatus.psa.completed
            ? "active"
            : "locked"
      } animate-fade-in-up`}
      style="animation-delay: 0.4s;"
      onclick={onboardingStatus.psa.completed &&
      !onboardingStatus.caqh.completed
        ? "window.location.href='/dashboard/onboarding/caqh'"
        : ""}
    >
      <div class="flex items-start">
        <div
          class={`step-number ${
            onboardingStatus.caqh.completed
              ? "bg-white/20"
              : onboardingStatus.psa.completed
                ? "bg-white/20"
                : "bg-gray-300"
          }`}
        >
          {
            onboardingStatus.caqh.completed ? (
              <svg
                width="20"
                height="20"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"
                />
              </svg>
            ) : (
              <span>3</span>
            )
          }
        </div>
        <div class="flex-1">
          <h3
            class="text-xl font-bold mb-2"
            style="font-family: 'Manrope', sans-serif;"
          >
            CAQH Credentialing
          </h3>
          <p class="mb-4 opacity-90">
            Connect your CAQH profile and complete additional credentialing
            requirements.
          </p>
          <div class="space-y-2 text-sm opacity-80">
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                ></path>
              </svg>
              CAQH ProView integration
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12l2 2 4-4m5.414-4.414a2 2 0 00-2.828 0L4 16.172V20h3.828l10.586-10.586a2 2 0 000-2.828z"
                ></path>
              </svg>
              Automated verification
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Est. 15-20 minutes
            </div>
          </div>
          {
            !onboardingStatus.psa.completed && (
              <div class="mt-4 text-sm opacity-60">🔒 Complete PSA first</div>
            )
          }
          {
            onboardingStatus.caqh.completed && (
              <div class="mt-4 text-sm">
                ✅ Completed on {onboardingStatus.caqh.completedDate}
              </div>
            )
          }
        </div>
      </div>
    </div>

    <!-- Step 4: Banking Information -->
    <div
      class={`usrad-card p-8 step-card ${
        onboardingStatus.banking.completed
          ? "completed"
          : onboardingStatus.caqh.completed
            ? "active"
            : "locked"
      } animate-fade-in-up`}
      style="animation-delay: 0.5s;"
      onclick={onboardingStatus.caqh.completed &&
      !onboardingStatus.banking.completed
        ? "window.location.href='/dashboard/onboarding/banking'"
        : ""}
    >
      <div class="flex items-start">
        <div
          class={`step-number ${
            onboardingStatus.banking.completed
              ? "bg-white/20"
              : onboardingStatus.caqh.completed
                ? "bg-white/20"
                : "bg-gray-300"
          }`}
        >
          {
            onboardingStatus.banking.completed ? (
              <svg
                width="20"
                height="20"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"
                />
              </svg>
            ) : (
              <span>4</span>
            )
          }
        </div>
        <div class="flex-1">
          <h3
            class="text-xl font-bold mb-2"
            style="font-family: 'Manrope', sans-serif;"
          >
            Banking Information
          </h3>
          <p class="mb-4 opacity-90">
            Set up your payment details for fast and secure reimbursements.
          </p>
          <div class="space-y-2 text-sm opacity-80">
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"
                ></path>
              </svg>
              ACH direct deposit
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"
                ></path>
              </svg>
              Bank-level security
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Est. 5 minutes
            </div>
          </div>
          {
            !onboardingStatus.caqh.completed && (
              <div class="mt-4 text-sm opacity-60">
                🔒 Complete credentialing first
              </div>
            )
          }
          {
            onboardingStatus.banking.completed && (
              <div class="mt-4 text-sm">
                ✅ Completed on {onboardingStatus.banking.completedDate}
              </div>
            )
          }
        </div>
      </div>
    </div>

    <!-- Step 5: Welcome & Setup -->
    <div
      class={`usrad-card p-8 step-card ${
        onboardingStatus.welcome.completed
          ? "completed"
          : onboardingStatus.banking.completed
            ? "active"
            : "locked"
      } animate-fade-in-up`}
      style="animation-delay: 0.6s;"
      onclick={onboardingStatus.banking.completed &&
      !onboardingStatus.welcome.completed
        ? "window.location.href='/dashboard/onboarding/welcome'"
        : ""}
    >
      <div class="flex items-start">
        <div
          class={`step-number ${
            onboardingStatus.welcome.completed
              ? "bg-white/20"
              : onboardingStatus.banking.completed
                ? "bg-white/20"
                : "bg-gray-300"
          }`}
        >
          {
            onboardingStatus.welcome.completed ? (
              <svg
                width="20"
                height="20"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"
                />
              </svg>
            ) : (
              <span>5</span>
            )
          }
        </div>
        <div class="flex-1">
          <h3
            class="text-xl font-bold mb-2"
            style="font-family: 'Manrope', sans-serif;"
          >
            Welcome & Dashboard Setup
          </h3>
          <p class="mb-4 opacity-90">
            Complete your profile and explore your provider dashboard features.
          </p>
          <div class="space-y-2 text-sm opacity-80">
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                ></path>
              </svg>
              Profile completion
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"
                ></path>
              </svg>
              Tutorial & resources
            </div>
            <div class="flex items-center">
              <svg
                class="w-4 h-4 mr-2"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Est. 10 minutes
            </div>
          </div>
          {
            !onboardingStatus.banking.completed && (
              <div class="mt-4 text-sm opacity-60">
                🔒 Complete banking setup first
              </div>
            )
          }
          {
            onboardingStatus.welcome.completed && (
              <div class="mt-4 text-sm">
                ✅ Completed on {onboardingStatus.welcome.completedDate}
              </div>
            )
          }
        </div>
      </div>
    </div>
  </div>

  <!-- Support Section -->
  <div class="usrad-card p-8 animate-fade-in-up" style="animation-delay: 0.6s;">
    <div class="flex items-center justify-between">
      <div>
        <h3
          class="text-xl font-bold usrad-navy mb-2"
          style="font-family: 'Manrope', sans-serif;"
        >
          Need Help?
        </h3>
        <p class="text-gray-600">
          Our onboarding team is here to assist you every step of the way.
        </p>
      </div>
      <div class="flex space-x-4">
        <button
          onclick="window.open('mailto:onboarding@usradiology.com', '_blank')"
          class="px-6 py-3 usrad-gradient-navy text-white font-semibold rounded-lg shadow-lg hover:shadow-xl transition-all duration-300"
        >
          <svg
            class="w-5 h-5 mr-2 inline"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
            ></path>
          </svg>
          Email Support
        </button>
        <button
          onclick="window.open('tel:+1-855-USRADIO', '_blank')"
          class="px-6 py-3 border-2 border-usrad-gold text-usrad-gold font-semibold rounded-lg hover:bg-usrad-gold hover:text-white transition-all duration-300"
        >
          <svg
            class="w-5 h-5 mr-2 inline"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
            ></path>
          </svg>
          Call Us
        </button>
      </div>
    </div>
  </div>

  <!-- Completion Banner (show when all steps done) -->
  {
    completedSteps === totalSteps && (
      <div
        class="usrad-card p-8 usrad-gradient-gold text-white text-center animate-fade-in-up"
        style="animation-delay: 0.7s;"
      >
        <div class="flex items-center justify-center mb-4">
          <svg
            class="w-16 h-16 mr-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
            />
          </svg>
          <div>
            <h2
              class="text-3xl font-bold"
              style="font-family: 'Manrope', sans-serif;"
            >
              Welcome to USRad! 🎉
            </h2>
            <p class="text-yellow-100 text-lg">
              Your onboarding is complete. You're ready to start providing
              services.
            </p>
          </div>
        </div>
        <button
          onclick="window.location.href='/dashboard'"
          class="px-8 py-4 bg-white text-usrad-gold font-bold rounded-lg shadow-lg hover:shadow-xl transition-all duration-300 text-lg"
        >
          Go to Dashboard →
        </button>
      </div>
    )
  }
</DashboardLayout>
