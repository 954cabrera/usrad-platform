---
import MainLayout from "../layouts/MainLayout.astro";
import AOSInit from "../components/AOSInit.astro";
---

<MainLayout>
  <AOSInit />

  <!-- Hero Section -->
  <section
    class="bg-gradient-to-b from-[#fcf9f0] to-[#f5e7c5] text-[#003087] py-24 px-6 sm:px-12 text-center"
  >
    <h1 class="text-5xl md:text-6xl font-extrabold tracking-tight mb-4">
      A New Kind of Managed Care
    </h1>

    <div
      class="text-3xl md:text-4xl font-bold mb-8 flex justify-center items-center gap-2 tracking-tight"
    >
      <span>Built for the</span>
      <span class="relative h-[40px] w-[12ch] overflow-hidden inline-block">
        <span
          id="rotating-container"
          class="absolute top-0 left-0 font-extrabold text-[#cc9933]"
        >
          <span
            class="block text-left text-[#cc9933] font-extrabold h-[40px] leading-[40px]"
            >Uninsured</span
          >
          <span
            class="block text-left text-[#cc9933] font-extrabold h-[40px] leading-[40px]"
            >Underinsured</span
          >
          <span
            class="block text-left text-[#cc9933] font-extrabold h-[40px] leading-[40px]"
            >Uninsured</span
          >
        </span>
      </span>
    </div>

    <p class="text-xl max-w-2xl mx-auto mb-8">
      USRad is the first managed care platform dedicated to ancillary
      services—starting with diagnostic imaging—for the 90 million uninsured and
      underinsured Americans.
    </p>
    <div class="flex justify-center gap-4 flex-wrap">
      <a
        href="#partner"
        class="bg-[#e6c378] hover:bg-[#cc9933] text-white px-6 py-3 rounded-xl font-semibold"
        >Request Partner Deck</a
      >
      <a
        href="#network"
        class="border border-[#003087] text-[#003087] hover:bg-[#003087] hover:text-white px-6 py-3 rounded-xl font-semibold"
        >Join the Imaging Network</a
      >
      <a
        href="#model"
        class="border border-[#003087] text-[#003087] hover:bg-[#003087] hover:text-white px-6 py-3 rounded-xl font-semibold"
        >Learn About the Model</a
      >
    </div>
  </section>

  <script>
    const container = document.getElementById("rotating-container");
    const wordHeight = 40;
    let index = 0;

    setInterval(() => {
      index++;
      container.style.transition = "transform 0.5s ease-in-out";
      container.style.transform = `translateY(-${index * wordHeight}px)`;

      if (index === 2) {
        setTimeout(() => {
          container.style.transition = "none";
          container.style.transform = "translateY(0)";
          index = 0;
        }, 600);
      }
    }, 3000);
  </script>

  <script>
    const container = document.getElementById("rotating-container");
    const wordHeight = 40;
    let index = 0;

    setInterval(() => {
      index++;
      container.style.transition = "transform 0.5s ease-in-out";
      container.style.transform = `translateY(-${index * wordHeight}px)`;

      if (index === 2) {
        setTimeout(() => {
          container.style.transition = "none";
          container.style.transform = "translateY(0)";
          index = 0;
        }, 600);
      }
    }, 3000);
  </script>

  <!-- Problem Section -->
  <section class="py-20 bg-white text-[#003087] px-6 sm:px-12">
    <h2 class="text-3xl font-bold text-center mb-12">
      The System Wasn’t Built for Self-Pay Patients
    </h2>
    <div class="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">
      <div class="bg-[#f9f5eb] p-6 rounded-xl shadow">
        <h3 class="text-xl font-semibold mb-4">Traditional Insurance</h3>
        <ul class="list-disc pl-6 space-y-2">
          <li>High deductibles</li>
          <li>Complex referrals</li>
          <li>Surprise bills</li>
          <li>Hospital markups</li>
          <li>Denials, delays</li>
        </ul>
      </div>
      <div class="bg-[#fefbf5] p-6 rounded-xl border border-[#e6c378]">
        <h3 class="text-xl font-semibold mb-4">USRad Managed Care</h3>
        <ul class="list-disc pl-6 space-y-2">
          <li>Flat-rate pricing</li>
          <li>Direct scheduling</li>
          <li>All-inclusive fees</li>
          <li>Independent centers</li>
          <li>Navigation support</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Platform Overview -->
  <section class="py-20 bg-[#fcf9f0] text-[#003087] px-6 sm:px-12">
    <h2 class="text-3xl font-bold text-center mb-12">The USRad Platform</h2>
    <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
      <div class="text-center">
        <img
          src="/icons/managed-care/network.png"
          alt="Network"
          class="mx-auto h-12 mb-4"
        />
        <h3 class="font-semibold text-lg mb-2">
          Nationwide Credentialed Network
        </h3>
        <p>
          High-field MRI/CT facilities, vetted for quality, licensure, and
          equipment.
        </p>
      </div>
      <div class="text-center">
        <img
          src="/icons/managed-care/pricing.png"
          alt="Pricing"
          class="mx-auto h-12 mb-4"
        />
        <h3 class="font-semibold text-lg mb-2">Flat-Rate Contracts</h3>
        <p>Patients pay one pre-negotiated price. No billing games.</p>
      </div>
      <div class="text-center">
        <img
          src="/icons/managed-care/radiology.png"
          alt="Radiologists"
          class="mx-auto h-12 mb-4"
        />
        <h3 class="font-semibold text-lg mb-2">
          Board-Certified Radiology Reads
        </h3>
        <p>Every scan is interpreted by a board-certified radiologist.</p>
      </div>
    </div>
  </section>

  <!-- Cost Comparison -->
  <section class="py-20 bg-white text-[#003087] px-6 sm:px-12 text-center">
    <h2 class="text-3xl font-bold mb-4">Why It Works</h2>
    <p class="text-lg mb-6">Hospital MRI: $3,500+ <br /> USRad: $600 Flat</p>
    <p class="text-xl font-semibold">Savings Per Scan: ~$2,900</p>
    <p class="text-md mt-4">Multiply that across 1M+ scans annually.</p>
  </section>

  <!-- Legacy Section -->
  <section class="py-20 bg-[#f5e7c5] text-[#003087] px-6 sm:px-12">
    <h2 class="text-3xl font-bold text-center mb-8">
      This Isn’t Our First Time
    </h2>
    <p class="max-w-4xl mx-auto text-center mb-8 text-lg">
      Our founder, Michael Cabrera, built AnciCare PPO into a $20M/year
      specialty network with:
    </p>
    <div class="grid md:grid-cols-2 gap-6 max-w-5xl mx-auto text-left">
      <ul class="list-disc pl-6">
        <li>1,200+ freestanding imaging centers</li>
        <li>3,000+ board-certified radiologists</li>
        <li>High-margin managed care contracts</li>
      </ul>
      <ul class="list-disc pl-6">
        <li>Labs & bloodwork</li>
        <li>Mobile diagnostics</li>
        <li>Home imaging & portable MRI</li>
        <li>Physical therapy & DME</li>
      </ul>
    </div>
  </section>

  <!-- Partner CTA -->
  <section
    class="py-20 bg-[#003087] text-white px-6 sm:px-12 text-center"
    id="partner"
  >
    <h2 class="text-3xl font-bold mb-6">Let’s Build the Future of Care</h2>
    <div class="flex flex-wrap justify-center gap-6">
      <a
        href="/locations"
        class="bg-[#e6c378] hover:bg-[#cc9933] px-6 py-3 rounded-xl font-semibold"
        >Find a Location</a
      >
      <a
        href="/partner"
        class="bg-white text-[#003087] hover:bg-[#003087] hover:text-white border border-white px-6 py-3 rounded-xl font-semibold"
        >Join Our Network</a
      >
      <a
        href="/contact"
        class="bg-white text-[#003087] hover:bg-[#003087] hover:text-white border border-white px-6 py-3 rounded-xl font-semibold"
        >Schedule a Call</a
      >
    </div>
  </section>
</MainLayout>
